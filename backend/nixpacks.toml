[phases.setup]
nixPkgs = ["python312"]

[phases.setup.aptPkgs]
pkgs = []

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python wsgi.py"

[variables]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"
